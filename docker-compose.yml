services:
  backend:
    build: .backend/
    ports:
      - "3000:3000"
    volumes:
      - ./backend/data:/usr/src/app/data
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend